#pragma once
#include"game.h"
#include"SDL.h"
#include"SDL_image.h"
#include"SDL_mixer.h"
#include<vector>
struct Vector2
{
	float x;
	float y;
};
struct Bullet {
	float x, y;
	float size;
};

struct Target {
	float x, y;
	float width, height;
	float speed;
	int direction; // 1なら右、-1なら左

};
struct kassi
{
	float x, y;
};

class game {
public:
	game();
	// Initialize the game
	bool Initialize();
	// Runs the game loop until the game is over
	void RunLoop();
	// Shutdown the game
	void Shutdown();
private:
	// Helper functions for the game loop
	void ProcessInput();
	void UpdateGame();
	void GenerateOutput();

	// Window created by SDL
	SDL_Window* mWindow;
	SDL_Window* mWindow2;
	// Renderer for 2D drawing
	SDL_Renderer* mRenderer;
	SDL_Renderer* mRenderer2;
	// Number of ticks since start of game
	Uint32 mTicksCount;
	// Game should continue to run
	bool mIsRunning;
	bool mIsRunning2;
	bool mrun;

	// Texture
	SDL_Texture* texture;
	SDL_Texture* mGunTexture;
	SDL_Texture* mGunTexture2;
	SDL_Texture* mBulletTexture;
	SDL_Texture* mTargetTexture;
	SDL_Texture* mAim1;
	SDL_Texture* mAim2;
	SDL_Texture* kassi1;
	SDL_Texture* kassi1_2;
	SDL_Texture* kassi2;
	SDL_Texture* kassi2_2;
	SDL_Texture* kassi2_3; 
	SDL_Texture* kassi2_4;
	SDL_Texture* kassi2_5;
	SDL_Texture* kassi3;
	SDL_Texture* kassi3_2;
	SDL_Texture* kassi3_3;
	SDL_Texture* kassi3_4;
	
	// Music
	Mix_Music* bgm;
	Mix_Music* bgm2;
	Mix_Chunk* soundEffect;
	Mix_Chunk* soundEffect2;
	Mix_Chunk* sakebi;
	Mix_Chunk* kakusei;
	Mix_Chunk* intro;

	// Game objects
	SDL_Point mMousePos;
	bool mIsFiring = false;
	std::vector<Bullet> mBullets;
	std::vector<Target> mTargets;
	Target mTarget;
	std::vector<kassi> MKassi;
	//score and timer
	int mScore1 = 0;
	int mScore2 = 0;

	float mTimer = 30.0f;
	float mTimer2 = 60.0f;
	float mTimerf = 7.0f;

	bool mIsGameOver;
	bool mIsGameOver2;
	bool mIsGameover3;
	bool mIsGameover4;

	//controller
	SDL_Joystick* mController1 = nullptr;
	SDL_Joystick* mController2 = nullptr;

	int mPlayer1X, mPlayer1Y;
	int mPlayer2X, mPlayer2Y;
};
